# SECRET_KEY
# This is the main encryption key used by the application.
# MUST be a 32-byte (256-bit) key in HEX format.
#
# To generate a secure key:
#   openssl rand -hex 32
#
# Never share this key publicly.
# Change only when resetting the entire installation.
#
SECRET_KEY="your_generated_hex_string_here"

# DATABASE_URL
# The connection string for your PostgreSQL database.
# Format:
#   postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
#
# Make sure the database is PostgreSQL (required).
# Example for local development:
#   postgresql://postgres:password@localhost:5432/studwork?schema=public
#
DATABASE_URL="postgresql://user:password@localhost:5432/studwork?schema=public"

# NODE_ENV
# The environment the application is running in.
# Accepted values: development, production
NODE_ENV="development"
